<form method="post" action="{{ path('app_paie_employe_delete', {'id': paie_employe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paie_employe.id) }}">
    <button class="btn">Delete</button>
</form>
