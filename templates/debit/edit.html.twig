<!DOCTYPE html>

<title>Edit Debit</title>

{% block body %}
    <h1>Edit Debit</h1>

    {{ include('debit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_debit_index') }}">back to list</a>

    {{ include('debit/_delete_form.html.twig') }}
{% endblock %}
