<!DOCTYPE html>

<title>ProduitVendu</title>

{% block body %}
    <h1>ProduitVendu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_vendu.id }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ produit_vendu.qty }}</td>
            </tr>
            <tr>
                <th>Createdby</th>
                <td>{{ produit_vendu.createdby }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ produit_vendu.createdAt ? produit_vendu.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_vendu_index') }}">back to list</a>

    <a href="{{ path('app_produit_vendu_edit', {'id': produit_vendu.id}) }}">edit</a>

    {{ include('produit_vendu/_delete_form.html.twig') }}
{% endblock %}
